# Goal M6 verification evidence — 2026-09-07

## Automated results

- Web-to-Mac inventory: 31 native routes, no duplicates, no route left on the Goal M2 placeholder.
- SwiftFormat: 61 files checked, no formatting changes required after the Goal M6 implementation.
- Swift package tests: Core 36 passed, Design 1 passed, Features 36 passed.
- Backend full regression: 1197 passed.
- Frontend full regression: 200 passed; TypeScript and Vite production build passed.
- Backend macOS contract gate: 33 passed, including authentication and capability handshake.
- SwiftPM arm64 Release build: passed.
- Xcode arm64 Release build with signing disabled: passed.
- Local application bundle: ad-hoc codesign verification passed; executable is arm64 only; App Sandbox and
  outbound network client are the only app entitlements.
- Privacy Manifest: valid plist and embedded in the local candidate.
- Local DMG: built successfully and SHA-256 verification passed.

## Runtime measurements

- A 10-second Time Profiler recording launched the local candidate on Apple Silicon. The trace completed at
  its time limit and its `hang-risks` and `potential-hangs` tables contained no recorded rows.
- The Leaks Instruments template did not terminate cleanly after its 10-second limit and was stopped after
  confirming both processes belonged to this verification run. A restricted command-line sample reported
  288 allocations classified as leaks (14,400 bytes), predominantly system framework/XPC objects; this is
  not sufficient evidence to classify the app leak-free, so interactive Instruments review remains pending.
- The pre-existing `/Applications/Stock Monitor.app` process was not stopped or modified.

## External/manual gates still open

- Xcode UI tests built successfully twice, but both runners timed out before entering the test case because
  macOS Developer Mode is disabled. No UI assertion failed. Enabling this host setting requires an explicit
  system-level change before the accessibility smoke test can run.
- Same-account Web/Mac golden flows are represented by the checked-in matrix but have not been executed with
  a staging credential. Every route remains marked `runtime_pending`; no exception is silently accepted.
- VoiceOver, Full Keyboard Access, display zoom, language-length, Reduce Motion/Transparency, Increase
  Contrast, network-switch, sleep/wake, server-restart, long-stream and multiwindow soak evidence remains manual.
- Developer ID signing, notarization, stapling, Gatekeeper public-distribution assessment and clean-device
  install/upgrade/rollback remain blocked by the Apple Developer Program/notary credential gate.

This file records observed evidence only. It does not declare Goal M6 complete while any gate above remains open.
