# Stock Monitor v0.3.3

发布日期：2026-07-22

本版本聚焦财务数据可靠性、公司级查看体验、新闻归档维护，以及异动报告生成的幂等性。

## 新增与改进

- **Yahoo Finance 三表快照**：新增利润表、资产负债表、现金流量表的年度/季度持久化快照；页面展示数据来源与最近同步时间，缺失字段明确显示“数据不足”。
- **过期新闻与归档清理**：增加过期新闻、历史财报数据和临时快照的清理与归档机制，临时公司快照默认保留一天，降低无效数据长期堆积。
- **公司快照**：新闻、基本面、财报、估值和 SEC 页面支持按公司建立临时快照，并可在自选股与临时查看对象之间快速切换。
- **公司跳转逻辑**：统一各公司相关页面的当前股票选择状态；从搜索结果或快照入口跳转后，新闻、基本面、三表、估值和 SEC 视图保持同一家公司上下文。
- **证券搜索与自动补全**：支持按股票代码或公司名称搜索证券，区分 Yahoo 与 Finnhub 标识，候选结果支持直接加入自选股或生成临时快照。
- **异动调查聚合**：同一公司在同一美东交易日内只保留一个代表性异动事件，前端对历史重复调查进行归并展示。
- **报告幂等生成**：为异动、盘前/盘后及财报相关报告强化唯一键与状态判断，避免同一事件重复生成报告。
- **选股预设**：整理常用选股组合入口，便于从预设快速进入对应公司及分析视图。

## 体验与兼容性

- 财务报表支持年度/季度切换，并可查看指标释义、英文名称及分析影响。
- 公司搜索候选框采用固定层级展示，兼容移动端、滚动跟随、向上展开、减少动画和高对比度模式。
- 保留现有 API 与数据库迁移兼容路径；后端任务继续按队列执行，新闻与报告流程保持批量处理。

## 变更范围

- 数据库迁移：`0016`–`0019`（新闻归档、三表快照、临时快照、证券实体）。
- 后端：新闻归档、市场数据、快照、证券搜索、报告/异动幂等逻辑。
- 前端：统一公司切换、三表展示、快照入口、调查归并与搜索自动补全。

## 部署提示

后端代码更新后请同时重建 `api`、`worker`、`sec-worker`、`beat`；前端代码更新后重建 `frontend`。启动 API 时会自动执行 Alembic 迁移，生产升级前请先备份数据库。

